services:
  <% name %>:
    image: ghcr.io/podmania/<% name %>:latest
    userns_mode: "keep-id"
    container_name: <% name %>
    ports:
      - 1234:1234
    volumes:
      - ./config:/config
      - ./data:/data
      - /etc/localtime:/etc/localtime:ro
    restart: unless-stopped
